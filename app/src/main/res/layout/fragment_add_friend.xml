<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:background="@drawable/inicio1">

    <EditText
        android:id="@+id/nombreAmigoAnadir"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="30dp"
        app:layout_constraintTop_toTopOf="parent"
        android:background="@drawable/text_edits_login_signup"
        android:hint="Nombre del Artista"
        android:textColorHint="@color/white"
        android:textColor="@color/white"
        android:paddingStart="15dp"/>

    <EditText
        android:id="@+id/puntuacionAmigoAnadir"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="24dp"
        android:textColor="@color/white"
        android:background="@drawable/text_edits_login_signup"
        android:hint="Comentario"
        android:paddingStart="15dp"
        android:textColorHint="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/addImagenAnadir" />

    <ImageView
        android:id="@+id/addImagenAnadir"
        android:layout_width="388dp"
        android:layout_height="258dp"
        android:layout_marginTop="16dp"
        android:src="@drawable/marcador"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.4"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/nombreAmigoAnadir" />

    <Button
        android:id="@+id/botonDeAnadir"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginTop="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:layout_editor_absoluteX="20dp"
        android:background="@color/purple_200"
        android:layout_marginBottom="20dp"
        android:text="Añadir"/>



</androidx.constraintlayout.widget.ConstraintLayout>